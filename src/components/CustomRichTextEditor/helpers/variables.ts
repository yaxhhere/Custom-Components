import { Decoratives } from "./types";

export const DecorativeIdentifiers: Record<Decoratives, string[]> = {
    'bold': [
        'b', 'strong'
    ],
    'underline': [
        'u'
    ],
    'italic': [
        'i'
    ]
}